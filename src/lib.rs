pub mod api;
mod bridge;
mod group;
mod light;

pub use bridge::Bridge;
pub use light::Light;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
